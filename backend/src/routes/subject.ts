import express from 'express';

const router = express.Router();

// Placeholder routes for subject modules
router.get('/', (req, res) => {
  res.json({ message: 'Subject routes - Coming soon!' });
});

export default router;
