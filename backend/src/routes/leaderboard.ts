import express from 'express';

const router = express.Router();

// Placeholder routes for leaderboards
router.get('/', (req, res) => {
  res.json({ message: 'Leaderboard routes - Coming soon!' });
});

export default router;
